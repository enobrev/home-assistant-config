
    - alias: "Deck Lights on During Party Mode"
      trigger:
          platform: state
          entity_id: input_number.darkness
      condition:
          condition: and
          conditions: 
              - condition: state
                entity_id: input_boolean.mode_home_party
                state: 'on'
              - condition: template
                value_template: '{{ states.input_number.darkness.state | int >= 5 }}'
      action:
          service: switch.turn_on
          data:
              entity_id: switch.deck_switch # find-switch-deck

    - alias: "Deck Lights off During Party Mode"
      trigger:
          platform: state
          entity_id: input_number.darkness
      condition:
          condition: and
          conditions: 
              - condition: state
                entity_id: input_boolean.mode_home_party
                state: 'on'
              - condition: template
                value_template: '{{ states.input_number.darkness.state | int < 5 }}'
      action:
          service: switch.turn_off
          data:
              entity_id: switch.deck_switch # find-switch-deck

